{
  "name": "pi-flash",
  "version": "1.1.2",
  "private": true,
  "description": "Game to memorize and reproduce the digits of π.",
  "scripts": {
    "dev": "webxdc-dev run src --no-csp",
    "pack": "mkdir -p dist && (cd src && zip -9 -r - *) > dist/app.xdc",
    "dev:xdc": "npm run pack && webxdc-dev run dist/app.xdc"
  },
  "devDependencies": {
    "webxdc-dev": "latest"
  }
}